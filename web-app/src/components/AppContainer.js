import PropTypes from 'prop-types';
import React from 'react';
import { connect } from 'react-redux';


export class AppContainer extends React.Component {
    render() {
        return (
            <div><h1>App main</h1></div>
        );
    }
}

export default  AppContainer;
